import React, { Component } from 'react';

function Header(){

  return(
    <div>
      <a href="#">Home</a>
      <a href="#">Wines</a>
      <i class="fas fa-sign-in-alt"></i>
    </div>

  )
}

export default Header;
